# debian-openbox
Debian openbox dotfiles
\
\
WM        = Openbox \
Bar       = Polybar \
Launcher  = Openbox's menu
\
\
# about pipewire.config
It's a config I use to start pipewire-pulse and wireplumber. For those using the default init system, it won't be necessary and should be deleted